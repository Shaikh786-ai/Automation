name: Create UDE (OIDC, async-safe)

on:
  workflow_dispatch:
    inputs:
      environment_name:
        description: "Name of the UDE environment"
        required: true
        type: string

      domain_name:
        description: "Unique domain name for the environment"
        required: true
        type: string

      user_email:
        description: "User email to grant access"
        required: true
        type: string

      environment_type:
        description: "Environment type"
        required: true
        default: Sandbox
        type: choice
        options:
          - Sandbox
          - Production
          - Trial

      region:
        description: "Dataverse region"
        required: true
        default: unitedstates
        type: string

      currency:
        description: "Currency"
        required: true
        default: USD
        type: string

      language:
        description: "Language code"
        required: true
        default: "1033"
        type: string

      templates:
        description: "Power Platform template"
        required: true
        default: D365_FinOps_Finance
        type: string

permissions:
  id-token: write
  contents: read

jobs:
  create-ude:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # =========================
      # Install CLI
      # =========================
      - name: Install Power Platform CLI
        uses: microsoft/powerplatform-actions/actions-install@v1.9.1

      # =========================
      # Create environment (DO NOT WAIT LONG)
      # =========================
      - name: Create UDE environment (async)
        uses: microsoft/powerplatform-actions/create-environment@v1.9.1
        continue-on-error: true
        with:
          app-id: ${{ secrets.CLIENT_ID }}
          tenant-id: ${{ secrets.PP_TENANT_ID }}

          name: ${{ github.event.inputs.environment_name }}
          environment-type: ${{ github.event.inputs.environment_type }}
          region: ${{ github.event.inputs.region }}
          currency: ${{ github.event.inputs.currency }}
          language: ${{ github.event.inputs.language }}
          domain-name: ${{ github.event.inputs.domain_name }}

          templates: ${{ github.event.inputs.templates }}

          max-async-wait-time: 30

          template-metadata-json: |
            {
              "PostProvisioningPackages": [
                {
                  "applicationUniqueName": "msdyn_FinanceAndOperationsProvisioningAppAnchor",
                  "parameters": "DevToolsEnabled=true|DemoDataEnabled=true"
                }
              ]
            }

      # =========================
      # AUTH (OIDC - no secrets)
      # =========================
      - name: Authenticate PAC CLI
        run: |
          pac auth create \
            --applicationId ${{ secrets.CLIENT_ID }} \
            --tenant ${{ secrets.PP_TENANT_ID }}

      # =========================
      # WAIT FOR ENVIRONMENT READY (POLLING STEP)
      # =========================
      - name: Poll environment until ready
        run: |
          echo "Waiting for environment to become READY..."

          for i in {1..60}
          do
            STATUS=$(pac admin list --output json \
              | jq -r '.[] | select(.DisplayName=="${{ github.event.inputs.environment_name }}") | .State')

            echo "Attempt $i - Status: $STATUS"

            if [ "$STATUS" == "Ready" ]; then
              echo "Environment is READY"
              exit 0
            fi

            sleep 60
          done

          echo "Timeout waiting for environment readiness"
          exit 1

      # =========================
      # SELECT ENVIRONMENT
      # =========================
      - name: Select environment
        run: |
          pac org select --environment ${{ github.event.inputs.environment_name }}

      # =========================
      # ASSIGN USER ACCESS
      # =========================
      - name: Assign System Administrator role
        run: |
          pac admin assign-user \
            --user "${{ github.event.inputs.user_email }}" \
            --role "System Administrator"

      # =========================
      # FINAL WAIT (FO sync buffer)
      # =========================
      - name: FO sync buffer wait
        run: |
          echo "Waiting for FO provisioning sync..."
          sleep 300

      # =========================
      # DONE
      # =========================
      - name: Completion
        run: |
          echo "UDE Environment created successfully"
          echo "User granted access: ${{ github.event.inputs.user_email }}"
