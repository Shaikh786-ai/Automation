name: Delete UDE (OIDC, no secrets)

on:
  workflow_dispatch:
    inputs:
      environment_name:
        description: "Enter Power Platform Environment Name"
        required: true
        type: string

permissions:
  id-token: write
  contents: read

jobs:
  delete-ude:
    runs-on: ubuntu-latest

    steps:

      # Checkout repository
      - uses: actions/checkout@v4

      # Install PAC CLI (reliable)
      - name: Install PAC CLI
        run: |
          dotnet tool install --global Microsoft.PowerApps.CLI.Tool
          echo "$HOME/.dotnet/tools" >> $GITHUB_PATH

      # Verify PAC
      - name: Verify PAC CLI
        run: pac help

      # Resolve Environment ID (FULLY ROBUST FIX)
      - name: Resolve Environment ID
        id: resolve-env
        shell: bash
        run: |
          ENV_NAME="${{ github.event.inputs.environment_name }}"

          echo "Searching for environment: $ENV_NAME"

          pac admin list --json > envs.json
          cat envs.json

          # Handles ALL PAC structures (flat + nested)
          ENV_ID=$(jq -r '
            (.value // .)[] 
            | select(
                (
                  .DisplayName // 
                  .displayName // 
                  .name // 
                  .properties.displayName
                ) == "'"$ENV_NAME"'"
              )
            | (
                .EnvironmentId // 
                .environmentId // 
                .id
              )
          ' envs.json | head -n 1)

          if [ -z "$ENV_ID" ] || [ "$ENV_ID" == "null" ]; then
            echo "Environment not found!"

            echo "Available environments (debug list):"
            jq -r '
              (.value // .)[] 
              | (.DisplayName // .displayName // .name // .properties.displayName)
            ' envs.json

            exit 1
          fi

          echo "Resolved Environment ID: $ENV_ID"
          echo "environment_id=$ENV_ID" >> $GITHUB_OUTPUT

      # Delete Environment
      - name: Delete UDE environment
        uses: microsoft/powerplatform-actions/delete-environment@v1
        with:
          app-id: ${{ secrets.CLIENT_ID }}
          tenant-id: ${{ secrets.PP_TENANT_ID }}
          environment: ${{ steps.resolve-env.outputs.environment_id }}
          cloud: Public
