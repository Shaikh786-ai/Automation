name: Create UDE Environment

on:
  workflow_dispatch:
    inputs:

      environment_name:
        description: "Name of the UDE environment"
        required: true
        type: string

      domain_name:
        description: "Unique domain name for the environment"
        required: true
        type: string

      environment_type:
        description: "Environment type"
        required: true
        default: Sandbox
        type: choice
        options:
          - Sandbox
          - Production
          - Trial
          - Developer

      region:
        description: "Dataverse region"
        required: true
        default: unitedstates
        type: choice
        options:
          - unitedstates
          - europe
          - asia
          - australia
          - india
          - japan
          - canada
          - unitedkingdom
          - southamerica
          - france
          - unitedarabemirates
          - germany
          - switzerland
          - norway
          - korea
          - southafrica
          - sweden

      currency:
        description: "Currency"
        required: true
        default: USD
        type: string

      language:
        description: "Language code"
        required: true
        default: "1033"
        type: string

      templates:
        description: "Power Platform template"
        required: true
        default: D365_FinOps_Finance
        type: string

      enable_dataverse:
        description: "Add Dataverse data store?"
        required: true
        default: "No"
        type: choice
        options:
          - "Yes"
          - "No"

permissions:
  id-token: write
  contents: read

jobs:
  create-ude:
    # runs-on: ubuntu-latest
    runs-on: self-hosted

    steps:

      - uses: actions/checkout@v4

      # =========================
      # Install Power Platform CLI
      # =========================
      - name: Install Power Platform CLI
        uses: microsoft/powerplatform-actions/actions-install@v1.9.1

      # =========================
      # Create Environment
      # =========================
      - name: Create UDE environment
        uses: microsoft/powerplatform-actions/create-environment@v1.9.1
        with:
          app-id: ${{ secrets.CLIENT_ID }}
          tenant-id: ${{ secrets.PP_TENANT_ID }}

          name: ${{ github.event.inputs.environment_name }}
          environment-type: ${{ github.event.inputs.environment_type }}
          region: ${{ github.event.inputs.region }}
          currency: ${{ github.event.inputs.currency }}
          language: ${{ github.event.inputs.language }}
          domain-name: ${{ github.event.inputs.domain_name }}
          templates: ${{ github.event.inputs.templates }}

          provision-database: ${{ github.event.inputs.enable_dataverse == 'Yes' }}

          # ✅ UPDATED ASYNC WAIT TIME
          max-async-wait-time: 1800

      # =========================
      # Authenticate PAC CLI
      # =========================
      - name: Authenticate PAC
        run: |
          pac auth create \
            --applicationId ${{ secrets.CLIENT_ID }} \
            --tenant ${{ secrets.PP_TENANT_ID }}

      # =========================
      # Wait for environment ready
      # =========================
      - name: Wait for environment readiness
        run: |
          echo "Waiting for environment to become READY..."

          for i in {1..60}
          do
            STATUS=$(pac admin list --output json \
              | jq -r '.[] | select(.DisplayName=="${{ github.event.inputs.environment_name }}") | .State')

            echo "Attempt $i - Status: $STATUS"

            if [ "$STATUS" == "Ready" ]; then
              echo "Environment READY"
              exit 0
            fi

            sleep 60
          done

          echo "Timeout waiting for environment"
          exit 1

      # =========================
      # Summary
      # =========================
      - name: Summary
        run: |
          echo "===================================="
          echo "UDE ENVIRONMENT CREATED SUCCESSFULLY"
          echo "Name: ${{ github.event.inputs.environment_name }}"
          echo "Type: ${{ github.event.inputs.environment_type }}"
          echo "Region: ${{ github.event.inputs.region }}"
          echo "Dataverse Enabled: ${{ github.event.inputs.enable_dataverse }}"
          echo "===================================="
